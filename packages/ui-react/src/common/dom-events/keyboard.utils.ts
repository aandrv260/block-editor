export const isEnterKey = (key: string): boolean => key === "Enter";
export const isBackspaceKey = (key: string): boolean => key === "Backspace";
export const isArrowUpKey = (key: string): boolean => key === "ArrowUp";
export const isArrowDownKey = (key: string): boolean => key === "ArrowDown";
